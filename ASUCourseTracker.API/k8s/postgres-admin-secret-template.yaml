# Postgres Admin Secret for Migrations (template rendered by envsubst in CI)
apiVersion: v1
kind: Secret
metadata:
  name: postgres-admin-secrets
  namespace: asu-course-tracker
type: Opaque
stringData:
  POSTGRES_USER: "${POSTGRES_USER}"
  POSTGRES_PASSWORD: "${POSTGRES_PASSWORD}"

